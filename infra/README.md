# Infrastructure Directory

ไดเรกทอรีนี้รวบรวมการออกแบบและตั้งค่าโครงสร้างพื้นฐาน (Infrastructure Setup) สำหรับโปรเจกต์นี้ โดยมุ่งเน้นการประยุกต์ใช้แนวคิดสำคัญด้านระบบกระจาย (Distributed Systems) และสถาปัตยกรรมสมัยใหม่

| หัวข้อ | คำอธิบาย |
|--------|----------|
| **YAML Configuration** | การกำหนดค่าระบบแบบ declarative เช่น service definition, networking, deployment spec |
| **Containerization** | การรันแอปพลิเคชันใน isolated และ reproducible environment |
| **Orchestration** | การบริหาร container หลายตัวใน cluster — scaling, self-healing, service discovery |
| **Distributed Systems** | แนวคิด consistency, fault tolerance, communication model, resource coordination |
| **Workflow** | การออกแบบและรัน data pipeline ด้วย Argo Workflows |


## YAML คืออะไร?

**YAML** ย่อมาจาก *YAML Ain't Markup Language* เป็นภาษาสำหรับเขียนไฟล์ Configuration ที่อ่านง่าย ไม่รกสายตา ไม่ใช่ภาษาโปรแกรม แต่ใช้ **อธิบายโครงสร้างข้อมูล** (data structure)

```yaml
name: spark-job
version: 1.0
enabled: true
```

---

## ตัวอย่าง: Argo Workflow Hello World

ไฟล์: `hello-workflow.yaml`

```yaml
apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: hello-world-
  namespace: argo
spec:
  entrypoint: hello
  templates:
    - name: hello
      container:
        image: alpine:3.18
        command: [echo]
        args: ["Hello Argo Workflows!"]
```

### รัน Workflow

```bash
kubectl apply -f hello-workflow.yaml
```

ดูสถานะ:

```bash
kubectl get workflows -n argo
```

ดู log:

```bash
kubectl logs -n argo @latest
```

> หรือเปิดดูผ่าน **Argo UI** ได้โดยตรง

---

## อธิบาย YAML ทีละส่วน

### `apiVersion` / `kind`

```yaml
apiVersion: argoproj.io/v1alpha1
kind: Workflow
```

บอก Kubernetes ว่า resource นี้เป็นของ **Argo** และประเภทคือ **Workflow**

> - Spark ใช้ `kind: SparkApplication`  
> - Pod ทั่วไปใช้ `kind: Pod`

---

### `metadata`

```yaml
metadata:
  generateName: hello-world-
  namespace: argo
```

- `generateName` → Kubernetes สร้างชื่ออัตโนมัติ เช่น `hello-world-abcde`
- `namespace` → Workflow จะอยู่ใน namespace `argo`

---

### `spec` — หัวใจของ Workflow

```yaml
spec:
  entrypoint: hello
```

- `entrypoint` คือ **จุดเริ่มต้น** ของ Workflow
- ต้องตรงกับชื่อ template ที่กำหนดด้านล่าง
- เปรียบได้กับ `main()` ในโปรแกรมทั่วไป

---

### `templates`

```yaml
templates:
  - name: hello
```

- `templates` คือชุด "พิมพ์เขียว" ของแต่ละขั้นตอน
- แต่ละ template = **1 step / 1 pod**

---

### `container` — Step-as-a-Pod

```yaml
container:
  image: alpine:3.18
  command: [echo]
  args: ["Hello Argo Workflows!"]
```

สร้าง Pod จาก image `alpine` แล้วรันคำสั่ง:

```bash
echo "Hello Argo Workflows!"
```

> เมื่อ Pod รันเสร็จ → step สำเร็จ → workflow จบ

---

## 🏛️ มุมมองเชิง Architecture

```
Argo Workflow
     │
     ▼
Template: hello
     │
     ▼
Pod (alpine)
     │
     ▼
echo "Hello Argo Workflows!"
```

---

## Security Best Practices: AWS Credentials

### ❌ วิธีที่ไม่ควรทำ (`pipeline1.yaml`)

```yaml
hadoopConf:
  "fs.s3a.access.key": "Not Secure"
  "fs.s3a.secret.key": "Not Secure"
```

การฝัง credentials โดยตรงใน YAML **ไม่ปลอดภัย** เพราะค่าเหล่านี้จะถูกเก็บใน version control และมองเห็นได้ทุกคน

---

### วิธีที่แนะนำ (`pipeline2.yaml`)

```yaml
hadoopConf:
  "fs.s3a.impl": "org.apache.hadoop.fs.s3a.S3AFileSystem"
  "fs.s3a.endpoint": "s3.ap-southeast-1.amazonaws.com"
  "fs.s3a.path.style.access": "false"
  "fs.s3a.aws.credentials.provider": "com.amazonaws.auth.EnvironmentVariableCredentialsProvider"
```

ใช้ **Environment Variable Credentials Provider** แทนการฝัง credentials โดยตรง โดย credentials จะถูกดึงจาก Kubernetes Secret อัตโนมัติ

---

### สร้าง Kubernetes Secret สำหรับ AWS Credentials

```bash
kubectl create secret generic aws-creds -n spark-apps \
  --from-literal=aws-access-key=<AWS_ACCESS_KEY_ID> \
  --from-literal=aws-secret-key=<AWS_SECRET_ACCESS_KEY>
```

หากสำเร็จจะแสดง:

```
secret/aws-creds created
```

> ⚠️ **อย่าเก็บ Access Key จริงใน code repository** ให้ใช้ Kubernetes Secret หรือ secret management tool เช่น AWS Secrets Manager, HashiCorp Vault เสมอ


---

# Containerization คืออะไร

คือแนวคิดการบรรจุแอปพลิเคชันพร้อมกับสภาพแวดล้อมที่จำเป็นทั้งหมด (Libraries, Dependencies, Configurations) เข้าไว้ด้วยกันในแพ็กเกจเดียวที่เรียกว่า "Container" 
เพื่อให้มั่นใจว่าแอปพลิเคชันจะทำงานได้เหมือนกันทุกประการ ไม่ว่าจะรันบนเครื่องของ Developer, เครื่องทดสอบ (Staging) หรือบน Server จริง (Production)

ก่อนจะมาถึงจุดนี้ เราผ่านมา 2 ยุคหลักๆ คือ
- ยุค Traditional Deployment (Hardware-based)
ในยุคแรก เราติดตั้ง Software ลงบน Server (Physical Machine) โดยตรง
ปัญหาที่พบบ่อยๆ "Matrix from Hell" เมื่อแอปพลิเคชัน A ต้องการ Library เวอร์ชัน 1 แต่แอปพลิเคชัน B ต้องการเวอร์ชัน 2 บนเครื่องเดียวกัน 
ทำให้เกิดความขัดแย้ง (Conflict) และการจัดการทรัพยากรที่ไม่คุ้มค่า (เครื่องเดียวรันได้ไม่กี่อย่าง)

- ยุค Virtualization (VM)
VM (Virtual Machine) คือเทคโนโลยีการจำลอง "คอมพิวเตอร์ทั้งเครื่อง" ขึ้นมาบน Physical Server เครื่องเดียว 
โดยใช้ซอฟต์แวร์ที่เรียกว่า Hypervisor (เช่น VMware, VirtualBox) เป็นตัวจัดการ 

## VM คืออะไร?
VM เปรียบเสมือนการแบ่งเค้กออกมาเป็นก้อนๆ โดยที่แต่ละก้อนมีองค์ประกอบครบถ้วนในตัวเอง:
- Virtual Hardware: มีการจอง RAM, CPU และ Disk พื้นที่แยกขาดจากกันชัดเจน
- Guest OS: ในทุกๆ VM จะต้องมีการติดตั้ง Operating System (OS) ของตัวเอง (เช่น Windows, Linux) ลงไปข้างใน
- Isolation: มีความปลอดภัยสูงมากเพราะแต่ละ VM แยกจากกันโดยสิ้นเชิง (Hardware-level isolation)
เนื่องจากต้องรัน OS เต็มรูปแบบในทุกๆ VM ทำให้กินทรัพยากรเครื่องสูง (Heavyweight) 
และใช้เวลานานในการเปิดเครื่อง (Boot time) เพราะต้องรอ OS โหลดเสร็จก่อน

```text
Hypervisor
   |
   |--- VM-1
   |      ├── Guest OS (Ubuntu)
   |      └── Web Application
   |
   |--- VM-2
   |      ├── Guest OS (CentOS)
   |      └── API Service
   |
   |--- VM-3
          ├── Guest OS (Ubuntu)
          └── Database Service
```

ในยุค Containerization คือการทำ OS-level Virtualization
แทนที่จะจำลอง “เครื่องทั้งเครื่อง” แบบ VM
Container จะจำลองเฉพาะ process + runtime environment

จุดต่างสำคัญคือ:
Container แชร์ Kernel เดียวกันกับ Host OS

```text
[ Physical Hardware ]
             |
    [ Host Operating System (Linux Kernel) ]
             |
    [ Container Runtime (Docker / containerd) ]
             |
   -----------------------------------------
   |                |                      |
[ Container-1 ]  [ Container-2 ]        [ Container-3 ]
  (Web App)      (API Service)           (Database)
```

แม้ว่า Container จะแชร์ Kernel เดียวกันกับ Host แต่เรายังสามารถรัน Image ที่เป็นคนละ Distribution ได้ (เช่น Host เป็น Ubuntu แต่รัน Container เป็น Alpine หรือ CentOS) เพราะโครงสร้างของ Linux แบ่งออกเป็น 2 ส่วนหลัก:
1. Kernel (หัวใจของระบบ): ทำหน้าที่จัดการ Hardware, Memory และ CPU (ส่วนนี้ Container ใช้ร่วมกับ Host)
2. User Space (รูปลักษณ์ของระบบ): คือส่วนของ Libraries, Package Manager และ Utilities ต่างๆ (เช่น apt, yum, apk)

เมื่อคุณรัน Alpine Container บน Ubuntu Host ตัว Container จะนำแค่ User Space (ไฟล์ระบบขนาดเล็กของ Alpine) มาใช้งาน แต่คำสั่งเชิงลึกในการจัดการ Hardware จะยังคงวิ่งไปเรียกใช้ Kernel ของ Ubuntu Host นั่นเอง

# Oreshtration คืออะไร
หาก Container คือ "เครื่องดนตรี" แต่ละชิ้นที่ทำงานได้ด้วยตัวเอง Orchestration ก็คือ "วาทยากร" (Conductor) ที่คอยควบคุมให้เครื่องดนตรีนับร้อยนับพันชิ้นเล่นสอดประสานกันเป็นวงออเคสตราขนาดใหญ่ได้อย่างสมบูรณ์แบบ

ในระบบที่ซับซ้อน เราไม่สามารถสั่งรัน docker run ทีละคอนเทนเนอร์บนเซิร์ฟเวอร์หลายๆ เครื่องด้วยมือได้ เราจึงต้องการระบบมาจัดการเรื่องเหล่านี้แบบอัตโนมัติ

## หน้าที่หลักของ Orchestration
- Scheduling (การจัดสรรทรัพยากร)
ตัดสินใจว่า Container ไหนควรไปรันที่ Node (Server) เครื่องไหน โดยพิจารณาจาก CPU/RAM ที่เหลืออยู่ เพื่อให้ใช้ทรัพยากรคุ้มค่าที่สุด
- Self-healing (การซ่อมแซมตัวเอง)
หากมี Container ตัวไหน "ตาย" (Crash) หรือ Node ทั้งเครื่องพัง ระบบ Orchestration จะรู้ตัวทันทีและสั่งรัน Container ตัวใหม่ขึ้นมาทดแทนให้โดยอัตโนมัติเพื่อให้ระบบทำงานต่อได้ (High Availability)
- Scaling (การเพิ่ม-ลดขนาด)
เมื่อมี Traffic เข้ามาเยอะ ระบบสามารถสั่งเพิ่มจำนวน Container (Horizontal Scaling) เพื่อช่วยกันรับโหลด และลดจำนวนลงเมื่อไม่มีคนใช้งานเพื่อประหยัดค่าใช้จ่าย
- Service Discovery & Load Balancing
ทำหน้าที่เป็นจราจรคอยกระจาย Traffic ที่เข้ามาไปยัง Container ต่างๆ อย่างเท่าเทียม และช่วยให้ Container คุยกันเองได้ผ่านชื่อ Service (เช่น Web คุยกับ DB) โดยไม่ต้องระบุ IP Address ตรงๆ

# Distributed Systems & Distributed Computing
เวลาเราเจอกับปัญหาที่คอมพิวเตอร์เพียงเครื่องเดียวประมวลผลได้ช้าเกินไป หรือข้อมูลมีขนาดใหญ่เกินกว่าที่ทรัพยากรเครื่องเดียวจะรับไหว เรามักมี 2 ทางเลือกหลักในการขยายระบบ
1. Vertical Scaling (Scale-Up)
คือการเพิ่มประสิทธิภาพให้คอมพิวเตอร์เครื่องเดิม เช่น การเพิ่ม RAM, อัปเกรด CPU หรือขยาย Disk
- ข้อดี: จัดการง่าย (Simple) ไม่ต้องแก้ไข Code หรือสถาปัตยกรรมของ Software
- ข้อเสีย: มีเพดานจำกัด (Hardware Limit) และเมื่อสเปคสูงขึ้นเรื่อยๆ ราคาจะพุ่งสูงขึ้นแบบก้าวกระโดด (Exponential Cost) จนไม่คุ้มค่าเมื่อเทียบกับประสิทธิภาพที่ได้
2. Horizontal Scaling (Scale-Out)
คือการนำคอมพิวเตอร์สเปคทั่วไปหลายๆ เครื่องมาเชื่อมต่อกันเพื่อให้ช่วยกันทำงาน (Distributed System)
- ข้อดี: ขยายได้ไม่จำกัด (Unlimited Scalability) และ รักษาต้นทุน (Cost-Effective) ได้ดีกว่า เพราะเราสามารถใช้เครื่องสเปคธรรมดาราคาถูกหลายๆ เครื่องมาทำงานแทนเครื่อง Supercomputer 
ราคาแพงเพียงเครื่องเดียวได้
- ข้อเสีย: เกิด Overhead ในการบริหารจัดการ เช่น การติดต่อสื่อสารระหว่างโหนด (Network Latency), การคุมความถูกต้องของข้อมูล (Consistency) และความซับซ้อนในการเขียนโปรแกรม

## ทำไมต้อง Distributed Computing?
ในโปรเจกต์นี้ เราเลือกใช้แนวคิด Distributed Computing เพื่อแยกส่วนงาน (Tasks) กระจายไปยังหลายโหนดใน Cluster (เช่นการใช้ Spark บน Kubernetes) เพื่อบรรลุวัตถุประสงค์ดังนี้
- Fault Tolerance
- Parallel Processing
